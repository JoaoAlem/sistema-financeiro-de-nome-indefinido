{
  "scripts": {
    "db:generate": "bun --bun run drizzle-kit generate",
    "db:migrate": "bun --bun run drizzle-kit migrate",
    "db:seed": "bun --bun run src/seed.ts"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^0.31.10",
    "drizzle-seed": "^0.3.1",
    "tsx": "^4.21.0"
  }
}